type Suggestion = {
    id: String,
    title: String,
    description: String,
    category: Category,
    locked: Boolean,
    unread: Boolean,
    upvotes: Number,
    createdAt: Date,
    updatedAt: Number,
}