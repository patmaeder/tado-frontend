type Suggestion = {
    id: String,
    title: String,
    description: String,
    category: Category,
    locked: Boolean,
    unread: Boolean,
    upvoteCount: number,
    createdAt: Date,
    updatedAt: Number,
}